pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Hello World! ${GIT_BRANCH}  ,  ,  ${BUILD_NUMBER} ,   ${BUILD_URL}  ,  "
                mkdir testjenkins
            }
        }
         stage('Create Artifact') {
         steps {
            sh 'dir'
           }
        }
        
        
        
    }
}
