pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Hello World! ${GIT_BRANCH}  ,  ${BUILD_NUMBER}"
            }
        }
         stage('Create Artifact') {
         steps {
            sh 'ls'
           }
        }
        
        
        
    }
}
